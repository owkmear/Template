import React, { FC } from 'react'
import { Prediction } from '../../model'

type Props = {
  prediction: Prediction | null,
}

export const PredictionModal: FC<Props> = ({ prediction }) => {
  return <div></div>
}
